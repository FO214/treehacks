{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "agent-start-testing",
  "title": "AgentStartTesting",
  "description": "Server → Vision Pro: Agent starts testing",
  "type": "object",
  "required": ["type", "agent_id", "vercel_link", "browserbase_link"],
  "properties": {
    "type": { "const": "agent_start_testing", "description": "Message type" },
    "agent_id": {
      "type": "integer",
      "minimum": 1,
      "maximum": 9,
      "description": "Agent id 1-9 (position in 3x3 grid)"
    },
    "vercel_link": {
      "type": "string",
      "description": "Vercel deployment link"
    },
    "browserbase_link": {
      "type": "string",
      "description": "Browserbase session link"
    }
  }
}
